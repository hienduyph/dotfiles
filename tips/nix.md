# NIX

```bash
curl -Lo $HOME/.local/bin/nix-user-chroot 'https://github.com/nix-community/nix-user-chroot/releases/download/1.2.2/nix-user-chroot-bin-1.2.2-x86_64-unknown-linux-musl'
chmod +x $HOME/.local/bin/nix-user-chroot

nix-user-chroot $NIX_HOME zsh
```
